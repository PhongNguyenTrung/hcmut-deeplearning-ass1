# ──────────────────────────────────────────────────────────────────
# IMPORTANT: PyTorch must be installed separately with the correct index:
#
#   CPU only (recommended for local dev):
#   python -m pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
#
#   CUDA 12.1 (for GPU):
#   python -m pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#
#   Then install the rest:
#   python -m pip install -r requirements.txt --ignore-installed torch torchvision
# ──────────────────────────────────────────────────────────────────

# Core DL (installed separately via URL above)
# torch>=2.0.0
# torchvision>=0.15.0

# HuggingFace ecosystem
transformers>=4.35.0
datasets>=2.14.0
tokenizers>=0.14.0

# CLIP
openai-clip

# Interpretability
grad-cam>=1.4.8
captum>=0.6.0

# Demo
gradio>=4.0.0

# Data & Visualization
numpy>=1.24.0
pandas>=2.0.0
matplotlib>=3.7.0
seaborn>=0.12.0
scikit-learn>=1.8.0
Pillow>=10.0.0
tqdm>=4.65.0

# Logging (optional)
wandb

# Notebook
ipykernel
ipywidgets

# ── Object Detection (exercise_2) ─────────────────────────────────────────────
# YOLOv8 — one-stage detector với API dễ dùng cho người mới
ultralytics>=8.0.0
# COCO evaluation metrics (mAP@0.5, mAP@0.5:0.95) — thư viện chuẩn cho detection
pycocotools>=2.0.6
# OpenCV — xử lý ảnh trong Gradio demo và visualization
opencv-python>=4.8.0
